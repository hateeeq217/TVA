package com.tva.app.ui.theme

import androidx.compose.ui.graphics.Color

// TVA palette: calm, low-saturation, data-forward.
// Deliberately avoids default Material demo purples.

val InkBlack = Color(0xFF12141A)
val Charcoal = Color(0xFF1C1F27)
val Slate = Color(0xFF2A2E38)
val Mist = Color(0xFF8A909C)
val Paper = Color(0xFFF6F5F2)
val PaperDim = Color(0xFFEAE8E3)

val AccentTeal = Color(0xFF3AA6A0)     // primary — progress, positive change
val AccentAmber = Color(0xFFD98E4A)    // "approaching goal" — caution, never alarming
val AccentCoral = Color(0xFFC96B5C)    // "over target" — muted, not aggressive
val AccentGreen = Color(0xFF5FA777)    // improvement / under baseline
